{
  "watch": ["server", "src/lib"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "dist"],
  "exec": "ts-node --project tsconfig.server.json server/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
